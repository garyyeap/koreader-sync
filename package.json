{
  "name": "koreader-sync",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "npm run db:migrate:remote && wrangler deploy",
    "cf-typegen": "wrangler types",
    "db:migrate:local": "wrangler d1 migrations apply DB --local",
    "db:migrate:remote": "wrangler d1 migrations apply DB --remote"
  },
  "dependencies": {
    "hono": "^4.9.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260911.1",
    "typescript": "^5.9.2",
    "wrangler": "^4.131.1"
  },
  "cloudflare": {
    "bindings": {
      "DB": {
        "description": "D1 database that stores users and reading progress."
      },
      "PASSWORD_PEPPER": {
        "description": "Required secret. Generate with `openssl rand -hex 32`. Used to hash KOReader MD5 passwords before storage."
      },
      "REGISTRATION_ENABLED": {
        "description": "Set to `true` for first setup, then `false` after you create your account(s)."
      }
    }
  }
}
